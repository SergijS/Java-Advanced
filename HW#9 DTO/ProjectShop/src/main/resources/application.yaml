spring:
  jpa:
    hibernate:
      ddl-auto: 'update'
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/shop
    username: root
    password: Qrtye>>_1!
  flyway:
    baseline-on-migrate: true