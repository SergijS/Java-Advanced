spring:
  jpa:
    hibernate:
      ddl-auto: 'update'
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/shops
    username: root
    password: 332211
  flyway:
    baseline-on-migrate: true